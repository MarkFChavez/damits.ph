.card
  .card-image
    = image_tag(product.photo.url) if product.photo.present?
  .card-content
    %h5= product.name
  .card-action
    = link_to "Buy 1 for #{number_to_currency(product.price, unit: 'P')}", "#", class: "blue-text text-lighten-2"
